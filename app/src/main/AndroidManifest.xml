<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.triviaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TriviaApp">

        <!--STATS -->
        <activity android:name=".StatsActivity">
            android:label="@string/title_activity_statistics"
            android:theme="@style/Theme.TriviaApp.NoActionBar.Dialog"
        </activity>

        <!--SETTINGS -->
        <activity android:name=".SettingsActivity">
            android:label="@string/title_activity_settings">
        </activity>

        <!--SPLASH-->
        <activity android:name=".SplashActivity">
            <!-- android:theme="@style/Theme.Theme.TriviaApp.NoActionBar.SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>

        <!-- MAIN -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.TriviaApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>